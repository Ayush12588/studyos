{
  "name": "studyos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "echo 'Open index.html directly in browser for dev'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "terser": "^5.36.0",
    "clean-css": "^5.3.3"
  }
}