{
  "name": "studyos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.cjs",
    "dev": "echo 'Open app.html directly in browser for dev'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.1"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "esbuild": "^0.28.1",
    "terser": "^5.36.0"
  }
}
