{
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4"
  },
  "scripts": {
    "build": "npm run build:css && npm run build:js",
    "build:css": "cleancss -o styles.min.css styles.css",
    "build:js": "terser app.js --compress --mangle --output app.min.js"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.39.0"
  }
}